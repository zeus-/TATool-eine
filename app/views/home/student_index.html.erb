<div class="row">
  <%= react_component 'StudentHrIndexPage', {tas: @available_tas} %>
  <%# react_component 'StudentHrs', {student_hrs: @hr_array.to_json } %>  
  <%#  -@available_tas.each do |ta| %>
<%#    .col-md-6 %>
<%#      %h2 Help Requests for #{ta.full_name} %>
<%#      -ta.help_requests.where("is_complete = false").each do |hr| %>
<%#        %h4 #{hr.description} %>
<%#        %h6 -Asked by: #{hr.student.full_name} %>
<%#      =simple_form_for [current_user, HelpRequest.new] do |f| %>
<%#        =f.input :description %>
<%#        =f.hidden_field :ta_user_id, :value => ta.id %>
<%#        =f.submit %>

</div>
<script>
  window.currentUser = <%= UserSerializer.new(current_user, root: false).to_json.html_safe %>
</script>
